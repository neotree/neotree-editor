import * as schema from '@/databases/pg/schema';

export const USER_ROLES = schema.roleNameEnum.enumValues;

export const SCRIPT_TYPES = schema.scriptTypeEnum.enumValues;

export const SCREEN_TYPES = schema.screenTypeEnum.enumValues;

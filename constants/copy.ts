export const WHY_DIAGNOSIS_OPTION_DISABLED = 'Diagnosis is disabled because this script already contains a diagnosis script (Script can only have one diagnosis screen).';

export const WHY_PROBLEMS_OPTION_DISABLED = 'Problems is disabled because this script already contains a problems script (Script can only have one problems screen).';
